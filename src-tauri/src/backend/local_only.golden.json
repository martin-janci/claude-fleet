{
  "add_host": "add_host is not available while this desktop is a window onto https://hub.example.com; registering a host is fleet administration, which the hub reserves for its own operator — add it there with `fleet-hub`",
  "add_project": "add_project is not available while this desktop is a window onto https://hub.example.com; it clones or adopts a checkout using this machine's SSH and GitHub credentials; add the project on the hub, then it appears here",
  "assets_inventory": "assets_inventory is not available while this desktop is a window onto https://hub.example.com; the asset catalog is a git checkout on the machine that owns the fleet, and the hub has no tool for this; work on the catalog there",
  "assets_scan_hosts": "assets_scan_hosts is not available while this desktop is a window onto https://hub.example.com; the hub has this as its scan_assets tool, but its result feeds an inventory panel built on the catalog checkout, which only the machine that owns the fleet has; call scan_assets on the hub, or scan from that machine",
  "catalog_add_resource": "catalog_add_resource is not available while this desktop is a window onto https://hub.example.com; the asset catalog is a git checkout on the machine that owns the fleet, and the hub has no tool for this; work on the catalog there",
  "catalog_apply_sync": "catalog_apply_sync is not available while this desktop is a window onto https://hub.example.com; the hub's apply_sync is master-only: a paired client is never the fleet's administrator, and a sync writes to every host over SSH; run the sync on the hub",
  "catalog_commit_pending": "catalog_commit_pending is not available while this desktop is a window onto https://hub.example.com; the asset catalog is a git checkout on the machine that owns the fleet, and the hub has no tool for this; work on the catalog there",
  "catalog_config": "catalog_config is not available while this desktop is a window onto https://hub.example.com; the asset catalog is a git checkout on the machine that owns the fleet, and the hub has no tool for this; work on the catalog there",
  "catalog_configure": "catalog_configure is not available while this desktop is a window onto https://hub.example.com; the asset catalog is a git checkout on the machine that owns the fleet, and the hub has no tool for this; work on the catalog there",
  "catalog_create_asset": "catalog_create_asset is not available while this desktop is a window onto https://hub.example.com; the asset catalog is a git checkout on the machine that owns the fleet, and the hub has no tool for this; work on the catalog there",
  "catalog_delete_asset": "catalog_delete_asset is not available while this desktop is a window onto https://hub.example.com; the asset catalog is a git checkout on the machine that owns the fleet, and the hub has no tool for this; work on the catalog there",
  "catalog_delete_layer": "catalog_delete_layer is not available while this desktop is a window onto https://hub.example.com; deleting a layer removes a file from the catalog's git checkout, which only the machine that owns the fleet has, and the hub exposes no layer-authoring tool; author on that machine",
  "catalog_delete_secret": "catalog_delete_secret is not available while this desktop is a window onto https://hub.example.com; the asset catalog is a git checkout on the machine that owns the fleet, and the hub has no tool for this; work on the catalog there",
  "catalog_get_asset": "catalog_get_asset is not available while this desktop is a window onto https://hub.example.com; the asset catalog is a git checkout on the machine that owns the fleet, and the hub has no tool for this; work on the catalog there",
  "catalog_import_host": "catalog_import_host is not available while this desktop is a window onto https://hub.example.com; the hub has this as its import_assets tool, but the import lands in the catalog's git checkout, which only the machine that owns the fleet has; call import_assets on the hub, or import on that machine",
  "catalog_last_sync": "catalog_last_sync is not available while this desktop is a window onto https://hub.example.com; the asset catalog is a git checkout on the machine that owns the fleet, and the hub has no tool for this; work on the catalog there",
  "catalog_layer_template": "catalog_layer_template is not available while this desktop is a window onto https://hub.example.com; a template is the first step of authoring a layer into the catalog's git checkout, and catalog_write_layer refuses here for want of that checkout; the hub exposes no layer-authoring tool, so author on the machine that owns the fleet",
  "catalog_lint_all": "catalog_lint_all is not available while this desktop is a window onto https://hub.example.com; the asset catalog is a git checkout on the machine that owns the fleet, and the hub has no tool for this; work on the catalog there",
  "catalog_lint_asset": "catalog_lint_asset is not available while this desktop is a window onto https://hub.example.com; the asset catalog is a git checkout on the machine that owns the fleet, and the hub has no tool for this; work on the catalog there",
  "catalog_list_assets": "catalog_list_assets is not available while this desktop is a window onto https://hub.example.com; the hub does serve this list (its read-only list_assets tool, open to any paired client), but the Assets panel is built on the catalog's configuration and git checkout, which only the machine that owns the fleet has; call list_assets on the hub, or browse the catalog on that machine",
  "catalog_list_layers": "catalog_list_layers is not available while this desktop is a window onto https://hub.example.com; the hub does serve this (its read-only list_layers tool), but the layer definitions live in the catalog's git checkout, which only the machine that owns the fleet has; call list_layers on the hub, or work on the catalog there",
  "catalog_list_secrets": "catalog_list_secrets is not available while this desktop is a window onto https://hub.example.com; the asset catalog is a git checkout on the machine that owns the fleet, and the hub has no tool for this; work on the catalog there",
  "catalog_load": "catalog_load is not available while this desktop is a window onto https://hub.example.com; the asset catalog is a git checkout on the machine that owns the fleet, and the hub has no tool for this; work on the catalog there",
  "catalog_plan_sync": "catalog_plan_sync is not available while this desktop is a window onto https://hub.example.com; the hub has this as its plan_sync tool, but the plan is shown in a sync panel built on the catalog checkout, which only the machine that owns the fleet has; call plan_sync on the hub, or plan on that machine",
  "catalog_propose_layers": "catalog_propose_layers is not available while this desktop is a window onto https://hub.example.com; the hub does serve this (its read-only propose_layers tool), but a proposal is only useful where the layers can then be written — the catalog's git checkout, which only the machine that owns the fleet has; call propose_layers on the hub, or propose on that machine",
  "catalog_push": "catalog_push is not available while this desktop is a window onto https://hub.example.com; the asset catalog is a git checkout on the machine that owns the fleet, and the hub has no tool for this; work on the catalog there",
  "catalog_remove_resource": "catalog_remove_resource is not available while this desktop is a window onto https://hub.example.com; the asset catalog is a git checkout on the machine that owns the fleet, and the hub has no tool for this; work on the catalog there",
  "catalog_repo_status": "catalog_repo_status is not available while this desktop is a window onto https://hub.example.com; the asset catalog is a git checkout on the machine that owns the fleet, and the hub has no tool for this; work on the catalog there",
  "catalog_resolve_preview": "catalog_resolve_preview is not available while this desktop is a window onto https://hub.example.com; the hub has a resolve_preview tool, but it answers a summary — kind, name and version per asset — while this command returns the full Resolution the UI renders, so routing it would silently drop every asset body; call resolve_preview on the hub for the summary, or resolve on the machine that owns the fleet",
  "catalog_set_host_layers": "catalog_set_host_layers is not available while this desktop is a window onto https://hub.example.com; the hub has a set_host_layers tool, but it is master-only — a host's layer assignment decides what the next apply_sync writes to its filesystem — and a paired client is never the master; set layers on the machine that owns the fleet",
  "catalog_set_secret": "catalog_set_secret is not available while this desktop is a window onto https://hub.example.com; the hub's set_secret is master-only: a paired client is never the fleet's administrator, and the sync secrets belong to the machine that runs the sync; set it on the hub",
  "catalog_spawn_author_session": "catalog_spawn_author_session is not available while this desktop is a window onto https://hub.example.com; the asset catalog is a git checkout on the machine that owns the fleet, and the hub has no tool for this; work on the catalog there",
  "catalog_template": "catalog_template is not available while this desktop is a window onto https://hub.example.com; the asset catalog is a git checkout on the machine that owns the fleet, and the hub has no tool for this; work on the catalog there",
  "catalog_update_asset": "catalog_update_asset is not available while this desktop is a window onto https://hub.example.com; the asset catalog is a git checkout on the machine that owns the fleet, and the hub has no tool for this; work on the catalog there",
  "catalog_write_layer": "catalog_write_layer is not available while this desktop is a window onto https://hub.example.com; writing a layer edits a file in the catalog's git checkout, which only the machine that owns the fleet has, and the hub exposes no layer-authoring tool; author on that machine",
  "check_local_prereqs": "check_local_prereqs is not available while this desktop is a window onto https://hub.example.com; the onboarding checklist is about running a fleet from this machine, which the hub is doing instead",
  "discard_kill_session": "discard_kill_session is not available while this desktop is a window onto https://hub.example.com; the hub exposes no tool that discards a worktree and kills in one step; use safe_kill_session, or do it from the hub",
  "discover_hosts": "discover_hosts is not available while this desktop is a window onto https://hub.example.com; it reads this machine's ~/.ssh/config, not the hub's — register hosts on the hub itself with `fleet-hub` or a standalone app",
  "dismiss_agent_session": "dismiss_agent_session is not available while this desktop is a window onto https://hub.example.com; use Kill instead: the hub's kill_session removes an inactive agent from the list exactly as this would. It is not routed here because the two differ on a WORKING agent, which this refuses and kill_session stops",
  "get_fleet_settings": "get_fleet_settings is not available while this desktop is a window onto https://hub.example.com; these settings drive the reconcile tick, the GC sweeper and the playbooks, which the hub runs and this app does not; read and change them on the hub",
  "hide_host": "hide_host is not available while this desktop is a window onto https://hub.example.com; hiding a host is fleet administration, which the hub reserves for its own operator — hide it there with `fleet-hub`",
  "inspect_safe_kill": "inspect_safe_kill is not available while this desktop is a window onto https://hub.example.com; it inspects the worktree over this machine's SSH connection and the hub exposes no tool for it; retire the session from the hub",
  "install_fleet_hook": "install_fleet_hook is not available while this desktop is a window onto https://hub.example.com; the hook it installs points at this app's control API, which is not running; install it from the hub",
  "list_account_usage": "list_account_usage is not available while this desktop is a window onto https://hub.example.com; this app does not poll account usage while a hub owns the fleet, so the cache is empty; read usage on the hub",
  "list_github_repos": "list_github_repos is not available while this desktop is a window onto https://hub.example.com; it runs `gh` over this machine's SSH connection to the host; browse repositories from the hub or a standalone app",
  "list_host_tokens": "list_host_tokens is not available while this desktop is a window onto https://hub.example.com; these are this app's own per-host tokens, not the hub's; list them on the hub",
  "list_host_worktrees": "list_host_worktrees is not available while this desktop is a window onto https://hub.example.com; it scans the host over this machine's SSH connection and caches what it finds; use list_worktrees, which the hub answers",
  "mcp_configure": "mcp_configure is not available while this desktop is a window onto https://hub.example.com; starting a second control API against a fleet the hub already owns is the failure remote mode exists to prevent; configure the hub's",
  "mcp_status": "mcp_status is not available while this desktop is a window onto https://hub.example.com; this app runs no embedded control API while a hub owns the fleet (Task 1 skips it); the hub is the control API",
  "probe_ssh_alias": "probe_ssh_alias is not available while this desktop is a window onto https://hub.example.com; it SSHes from this machine to preview a host for the Add-host dialog; the hub is the one that must be able to reach it",
  "provision_hosts": "provision_hosts is not available while this desktop is a window onto https://hub.example.com; it rewrites every host's hook block to report to this app; provision from the hub with `fleet-hub`",
  "pty_open": "pty_open is not available while this desktop is a window onto https://hub.example.com; the terminal attaches over this machine's SSH connection to the session's host; attach from that host, or from a standalone app",
  "purge_project": "purge_project is not available while this desktop is a window onto https://hub.example.com; it deletes Claude Code state on every host over this machine's SSH connections and the hub exposes no tool for it; purge from the hub",
  "refresh_account_usage": "refresh_account_usage is not available while this desktop is a window onto https://hub.example.com; it reads the account's usage over this machine's SSH connection to the host; refresh it on the hub",
  "remove_host": "remove_host is not available while this desktop is a window onto https://hub.example.com; removing a host is fleet administration, which the hub reserves for its own operator — remove it there with `fleet-hub`",
  "repair_session": "repair_session is not available while this desktop is a window onto https://hub.example.com; the hub's repair_session always runs the EXPLICIT repair, which may unregister a stale worktree entry, adopt a moved checkout and recreate a branch — this app will not turn an automatic pre-attach check into that; repair explicitly, or from the hub",
  "repo_checkout": "repo_checkout is not available while this desktop is a window onto https://hub.example.com; the hub exposes no git-write tool — a remote client must not stage or commit under a running agent; do it in the session, or from a standalone app",
  "repo_checkout_commit": "repo_checkout_commit is not available while this desktop is a window onto https://hub.example.com; the hub exposes no git-write tool — a remote client must not stage or commit under a running agent; do it in the session, or from a standalone app",
  "repo_commit_create": "repo_commit_create is not available while this desktop is a window onto https://hub.example.com; the hub exposes no git-write tool — a remote client must not stage or commit under a running agent; do it in the session, or from a standalone app",
  "repo_create_branch": "repo_create_branch is not available while this desktop is a window onto https://hub.example.com; the hub exposes no git-write tool — a remote client must not stage or commit under a running agent; do it in the session, or from a standalone app",
  "repo_delete_branch": "repo_delete_branch is not available while this desktop is a window onto https://hub.example.com; the hub exposes no git-write tool — a remote client must not stage or commit under a running agent; do it in the session, or from a standalone app",
  "repo_fetch": "repo_fetch is not available while this desktop is a window onto https://hub.example.com; the hub exposes no git-write tool — a remote client must not stage or commit under a running agent; do it in the session, or from a standalone app",
  "repo_pull": "repo_pull is not available while this desktop is a window onto https://hub.example.com; the hub exposes no git-write tool — a remote client must not stage or commit under a running agent; do it in the session, or from a standalone app",
  "repo_push": "repo_push is not available while this desktop is a window onto https://hub.example.com; the hub exposes no git-write tool — a remote client must not stage or commit under a running agent; do it in the session, or from a standalone app",
  "repo_stage": "repo_stage is not available while this desktop is a window onto https://hub.example.com; the hub exposes no git-write tool — a remote client must not stage or commit under a running agent; do it in the session, or from a standalone app",
  "repo_unstage": "repo_unstage is not available while this desktop is a window onto https://hub.example.com; the hub exposes no git-write tool — a remote client must not stage or commit under a running agent; do it in the session, or from a standalone app",
  "rotate_host_token": "rotate_host_token is not available while this desktop is a window onto https://hub.example.com; it re-provisions the host to report to this app; rotate the token on the hub",
  "session_activity": "session_activity is not available while this desktop is a window onto https://hub.example.com; it captures the session's pane over this machine's SSH connection; the hub's pane reads answer a different shape, so the live indicator is off in remote mode",
  "session_tool_detail": "session_tool_detail is not available while this desktop is a window onto https://hub.example.com; the hub exposes no tool for one tool call's input and result; the Conversation tab's tool lines still come from session_conversation",
  "set_account_nickname": "set_account_nickname is not available while this desktop is a window onto https://hub.example.com; the nickname lives in the hub's database and there is no tool to set it; rename the account on the hub",
  "set_fleet_setting": "set_fleet_setting is not available while this desktop is a window onto https://hub.example.com; these settings drive the reconcile tick, the GC sweeper and the playbooks, which the hub runs and this app does not; change them on the hub",
  "set_host_token_mode": "set_host_token_mode is not available while this desktop is a window onto https://hub.example.com; these are this app's own per-host tokens, not the hub's; change the mode on the hub",
  "tunnel_status": "tunnel_status is not available while this desktop is a window onto https://hub.example.com; the tunnels belong to the process that owns the fleet; check them on the hub",
  "upload_to_session": "upload_to_session is not available while this desktop is a window onto https://hub.example.com; the file is on this machine and the session's host is the hub's to reach; copy it there yourself, or drop it on a standalone app"
}
